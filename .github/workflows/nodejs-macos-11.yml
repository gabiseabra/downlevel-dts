name: Node CI

on: [push]

jobs:
  build:
    runs-on: macos-11.0

    steps:
      - uses: actions/checkout@v1
      - name: npm install, build, and test
        run: |
          npm ci
          npm run build --if-present
          npm test
        env:
          CI: true
